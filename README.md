# GymBeat

GymBeat es una aplicación con arquitectura separada en `backend` y `frontend`, enfocada en autenticación de usuarios (registro e inicio de sesión) como base del sistema.

## Estructura del proyecto

```bash
GymBeat/
├── backend/   # API, lógica de negocio y acceso a datos
└── frontend/  # Interfaz de usuario
